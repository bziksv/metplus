# Отчёт: сравнение архива test.metplus-vrn.ru.tar.gz с корнем проекта

Дата: 2026-07-12

## Что сделано

1. Архив распакован в `test/` (2.6 ГБ, ~138k файлов)
2. Проведено сравнение `test/` ↔ корень проекта
3. Недостающие файлы скопированы в корень

## Итог сравнения

| Метрика | Значение |
|---------|----------|
| Файлов в архиве (`test/`) | 137 414 |
| Файлов в корне | 137 419 |
| Совпадают | 137 401 |
| Отличались до синхронизации | 5 |
| **Не было в корне (из архива)** | **8** |

**Вывод:** архив полный, почти всё уже было в корне. Не попали в основную копию в основном **скрытые dot-файлы** (macOS/Finder при распаковке часто их не копирует) и **меню**.

Исключено из сравнения: `test/`, `.git`, кеш, `.local/`, дампы, сам архив.

## Файлы, которых не было в корне (скопированы)

| Файл | Размер | Назначение |
|------|--------|------------|
| `.top.menu.php` | 516 B | Верхнее меню (КАТАЛОГ, Прайс-лист…) — скопирован ранее |
| `.left.menu_ext.php` | 1149 B | Выпадающий каталог из iblock — скопирован ранее |
| `.left.menu.php` | 29 B | Пустой базовый left-menu — создан ранее |
| `.bottom.menu.php` | 339 B | Нижнее меню футера |
| `.bottom_useful.menu.php` | 179 B | Меню «Полезное» |
| `.htaccess` | 7983 B | Apache: редиректы, блокировка IP, ЧПУ |
| `.personal.menu.php` | 897 B | Меню личного кабинета |
| `.section.php` | 116 B | Свойства раздела сайта |
| `.access.php` | 184 B | Права доступа Bitrix (полная версия с сервера) |
| `upload/ПОРЕЗКА ПРАЙС.pdf` | 58 KB | Прайс |
| `upload/ПОРУБКА ПРАЙС.pdf` | 62 KB | Прайс |
| `upload/ПРАЙС ДОСТАВКИ.pdf` | 63 KB | Прайс |

Полный лог копирования: `docs/archive-copied-files.txt`

## Файлы только в корне (добавлены нами, не из архива)

- `.local/` — локальный dev (nginx, php-fpm, db)
- `scripts/` — setup-local-db, start-dev, stop-dev
- `docs/` — документация
- `bitrix/php_interface/dbconn.local.php` — локальная БД
- `bitrix/.settings.example.php`, `dbconn.example.php` — шаблоны без секретов
- `.cursorignore`, `.gitignore`

## Отличия в общих файлах (намеренные)

| Файл | Причина |
|------|---------|
| `bitrix/php_interface/dbconn.php` | Добавлен хук на `dbconn.local.php` |
| `bitrix/.settings.php` | Переключено на локальную БД 127.0.0.1 |
| `.gitignore` | Расширен для git |
| `.access.php` | Была урезанная версия — заменена серверной |

## Псевдо-отличия (не реальные пропуски)

3 PDF в `upload/` показываются как «только в архиве» и «только в корне» одновременно — это **разная Unicode-нормализация имён** (NFC/NFD) на macOS. Файлы физически есть.

## Рекомендации

- Папку `test/` не коммитить в git (эталон для сравнения, 2.6 ГБ)
- При следующей распаковке: `cp -a test/.[!.]* .` или `rsync -a test/ ./`
- Детальный diff: `docs/archive-diff-report.txt`
